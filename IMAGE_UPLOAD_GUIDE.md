# Руководство по загрузке изображений

## 📸 Система категорий изображений

При добавлении товара в админ-панели изображения разделены на три категории:

### 1. **Обложка (Main Image)**
- **Назначение:** Главное фото товара
- **Отображается:** 
  - На карточке товара на главной странице
  - Первое изображение в галерее на странице товара
- **Количество:** 1 изображение
- **Обязательно:** Да

### 2. **Спина (Back Image)**
- **Назначение:** Фото обратной стороны товара
- **Отображается:**
  - При наведении курсора на карточку товара на главной
  - Второе изображение в галерее на странице товара
- **Количество:** 1 изображение
- **Обязательно:** Нет

### 3. **Остальное (Other Images)**
- **Назначение:** Дополнительные фото товара
- **Отображается:**
  - В галерее на странице товара (после обложки и спины)
- **Количество:** Неограниченно
- **Обязательно:** Нет

## 🗄️ Хранение в базе данных

### Структура Product Schema
```typescript
{
  mainImage: String,        // Имя файла обложки
  backImage: String,        // Имя файла спины
  otherImages: [String],    // Массив имён файлов
  images: [String]          // Старый формат (для обратной совместимости)
}
```

### Пример данных
```json
{
  "mainImage": "1699876543210-123456789.jpg",
  "backImage": "1699876543211-987654321.jpg",
  "otherImages": [
    "1699876543212-111111111.jpg",
    "1699876543213-222222222.jpg"
  ]
}
```

## 📁 Хранение файлов

### Директория
```
backend/
  public/
    photos/
      1699876543210-123456789.jpg
      1699876543211-987654321.jpg
      ...
```

### Формат имени файла
```
{timestamp}-{random}.{extension}
```
Пример: `1699876543210-123456789.jpg`

### Раздача файлов
- **URL:** `http://localhost:4000/photos/{filename}`
- **Пример:** `http://localhost:4000/photos/1699876543210-123456789.jpg`

## 🔄 Миграция существующих данных

### Шаг 1: Миграция изображений
```bash
cd backend
node scripts/migrate-images.js
```

**Что делает скрипт:**
1. Находит все товары с полем `images` (старый формат)
2. Извлекает имена файлов из URL
3. Распределяет по категориям:
   - `images[0]` → `mainImage`
   - `images[1]` → `backImage`
   - `images[2+]` → `otherImages`
4. Обновляет документы в MongoDB

### Шаг 2: Очистка старого поля images
```bash
cd backend
node scripts/cleanup-old-images.js
```

**Что делает скрипт:**
1. Находит все товары с новыми полями (mainImage, backImage, otherImages)
2. Удаляет старое поле `images` из документов
3. Освобождает место в базе данных

### Пример миграции
**До:**
```json
{
  "images": [
    "https://silasvitera.up.railway.app/api/image/3050_1855_0.jpg",
    "https://silasvitera.up.railway.app/api/image/3050_1855_1.jpg",
    "https://silasvitera.up.railway.app/api/image/3050_1855_2.jpg"
  ]
}
```

**После:**
```json
{
  "mainImage": "3050_1855_0.jpg",
  "backImage": "3050_1855_1.jpg",
  "otherImages": ["3050_1855_2.jpg"],
  "images": [...]  // Сохраняется для обратной совместимости
}
```

## 🎨 Использование в админ-панели

### Добавление товара

1. **Откройте админ-панель:** `/admin/products`
2. **Нажмите:** "+ Добавить товар"
3. **Заполните основные поля** (название, цена, категория и т.д.)
4. **Загрузите изображения:**

   **Обложка:**
   - Нажмите "Выберите файл"
   - Выберите главное фото товара
   - Дождитесь загрузки
   - Превью появится автоматически

   **Спина:**
   - Нажмите "Выберите файл"
   - Выберите фото обратной стороны
   - Дождитесь загрузки

   **Остальное:**
   - Нажмите "Выберите файлы"
   - Можно выбрать несколько файлов сразу
   - Все файлы загрузятся автоматически
   - Превью всех фото появится в сетке

5. **Нажмите:** "Добавить"

### Редактирование товара

1. **Наведите на карточку товара**
2. **Нажмите:** "Редактировать"
3. **Изменяйте изображения:**
   - Для замены: загрузите новое изображение
   - Для удаления: нажмите ✕ на превью
   - Для добавления: загрузите дополнительные файлы
4. **Нажмите:** "Сохранить"

## 🔧 Технические детали

### Backend API

**Загрузка изображений:**
```http
POST /api/products/upload/images
Content-Type: multipart/form-data

files: File[]
category: 'main' | 'back' | 'other'
```

**Ответ:**
```json
{
  "files": [
    {
      "filename": "1699876543210-123456789.jpg",
      "originalName": "photo.jpg",
      "size": 245678,
      "category": "main"
    }
  ]
}
```

### Frontend Component

**ImageUpload Props:**
```typescript
{
  label: string;                    // Заголовок секции
  category: 'main' | 'back' | 'other';  // Категория
  multiple: boolean;                // Множественная загрузка
  currentImages: string[];          // Текущие изображения
  onUpload: (filenames: string[]) => void;  // Callback
}
```

**Использование:**
```tsx
<ImageUpload
  label="Обложка (главное фото) *"
  category="main"
  multiple={false}
  currentImages={['/photos/image.jpg']}
  onUpload={(filenames) => setMainImage(filenames[0])}
/>
```

## 📊 Отображение на фронтенде

### Главная страница (Home.tsx)
```typescript
const mainImg = product.mainImage 
  ? `/photos/${product.mainImage}` 
  : product.images?.[0];

const backImg = product.backImage 
  ? `/photos/${product.backImage}` 
  : product.images?.[1];

<ProductCard 
  image={mainImg}
  images={[mainImg, backImg]}
/>
```

### Страница товара (ProductDetail.tsx)
```typescript
const allImages = [];
if (product.mainImage) allImages.push(`/photos/${product.mainImage}`);
if (product.backImage) allImages.push(`/photos/${product.backImage}`);
if (product.otherImages) {
  allImages.push(...product.otherImages.map(img => `/photos/${img}`));
}

// Fallback to old format
const displayImages = allImages.length > 0 
  ? allImages 
  : product.images || [];
```

## ⚙️ Настройка

### Ограничения загрузки

**В `products-upload.controller.ts`:**
```typescript
FilesInterceptor('files', 20, {  // Максимум 20 файлов
  fileFilter: (req, file, callback) => {
    // Только изображения
    if (!file.mimetype.match(/\/(jpg|jpeg|png|gif|webp)$/)) {
      return callback(new BadRequestException('Only image files!'), false);
    }
    callback(null, true);
  },
  limits: {
    fileSize: 5 * 1024 * 1024,  // Максимум 5MB на файл
  },
})
```

### Изменение директории

**В `main.ts`:**
```typescript
app.useStaticAssets(join(__dirname, '..', 'public', 'photos'), {
  prefix: '/photos/',
});
```

## 🐛 Troubleshooting

### Изображения не загружаются
1. Проверьте, что папка `backend/public/photos` существует
2. Убедитесь, что у процесса есть права на запись
3. Проверьте размер файла (не более 5MB)
4. Убедитесь, что формат файла поддерживается (jpg, jpeg, png, gif, webp)

### Изображения не отображаются
1. Проверьте, что backend запущен на порту 4000
2. Откройте в браузере: `http://localhost:4000/photos/{filename}`
3. Проверьте консоль браузера на ошибки CORS
4. Убедитесь, что файл существует в папке `public/photos`

### Миграция не работает
1. Проверьте подключение к MongoDB
2. Убедитесь, что в базе есть товары с полем `images`
3. Проверьте формат URL в поле `images`
4. Посмотрите логи скрипта для деталей ошибок

## 💡 Рекомендации

### Для production
1. **CDN:** Используйте CDN для раздачи изображений (Cloudflare, AWS CloudFront)
2. **Оптимизация:** Сжимайте изображения перед загрузкой (Sharp, ImageMagick)
3. **Форматы:** Используйте WebP для лучшего сжатия
4. **Lazy loading:** Загружайте изображения по мере прокрутки
5. **Backup:** Регулярно делайте резервные копии папки `public/photos`

### Для лучшего UX
1. Показывайте прогресс загрузки
2. Добавьте drag-and-drop для загрузки
3. Позволяйте изменять порядок изображений
4. Добавьте предпросмотр перед загрузкой
5. Показывайте размер и разрешение изображения

## 📝 Checklist при добавлении товара

- [ ] Загружена обложка (главное фото)
- [ ] Загружено фото спины (если есть)
- [ ] Загружены дополнительные фото (если есть)
- [ ] Все изображения четкие и качественные
- [ ] Размер каждого файла не превышает 5MB
- [ ] Формат файлов: JPG, PNG, WebP
- [ ] Изображения отображаются в превью
- [ ] Товар сохранен успешно
