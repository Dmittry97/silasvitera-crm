# Сила свитера - Интернет-магазин свитеров ручной работы

🔗 **GitHub:** https://github.com/Dmittry97/silasvitera-crm

Современный e-commerce сайт на React + NestJS + MongoDB.

## 🚀 Технологии

### Frontend
- **React 18** + TypeScript
- **Webpack 5** + Dev Server
- **TailwindCSS** для стилизации
- **React Router** для навигации
- **Axios** для API запросов

### Backend
- **NestJS 10** + TypeScript
- **MongoDB** + Mongoose
- **Bcrypt** для хеширования паролей
- **Class Validator** для валидации
- **Axios** для внешних API

## 📦 Установка

### Backend
```bash
cd backend
npm install
```

Создайте файл `.env`:
```env
MONGODB_URI=mongodb://localhost:27017/silasvitera
PORT=4000
```

### Frontend
```bash
cd frontend
npm install
```

## 🏃 Запуск

### Backend (порт 4000)
```bash
cd backend
npm run start:dev
```

### Frontend (порт 5173)
```bash
cd frontend
npm run dev
```

## 📥 Импорт товаров

После запуска backend откройте в браузере:
```
http://localhost:4000/getsviter
```

Это загрузит товары из внешнего API и сохранит в вашу MongoDB.

## 🔐 Админ-панель

### Установка зависимостей
```bash
cd backend
npm install
```

### Генерация хеша пароля (опционально)
```bash
node scripts/hash-password.js your_password
```

### Доступ к админ-панели
1. Откройте `http://localhost:5173/admin/login`
2. Введите учетные данные:
   - **Логин**: `admin`
   - **Пароль**: `admin123`
3. После авторизации вы попадете в дашборд

### Возможности админ-панели
- **Заказы**: просмотр всех заказов, изменение статуса (Новый, В обработке, Отправлен, Доставлен, Отменен)
- **Товары**: 
  - Просмотр каталога с группировкой по категориям (4 колонки)
  - Фильтрация по категориям
  - Добавление новых товаров
  - Редактирование существующих товаров
  - Удаление товаров
- **Настройки**:
  - Управление категориями (добавление/удаление)
  - Управление размерами (добавление/удаление)
- **Статистика**: общее количество заказов, товаров, новых заказов

## 🌐 API Endpoints

### Products
- `GET /api/products` - Список всех товаров
- `GET /api/products/:id` - Один товар
- `POST /api/products` - Создать товар
- `PATCH /api/products/:id` - Обновить товар
- `DELETE /api/products/:id` - Удалить товар

### Orders
- `GET /api/orders` - Список всех заказов
- `GET /api/orders/:id` - Один заказ
- `POST /api/orders` - Создать заказ
- `PATCH /api/orders/:id` - Обновить заказ (статус)
- `DELETE /api/orders/:id` - Удалить заказ

### Auth
- `POST /api/auth/login` - Авторизация администратора

### Settings
- `GET /api/settings` - Получить настройки (категории, размеры)
- `PATCH /api/settings` - Обновить настройки

### Upload
- `POST /api/products/upload/images` - Загрузка изображений товаров

### Static Files
- `GET /photos/:filename` - Раздача загруженных изображений

### Import
- `GET /getsviter` - Импорт товаров из внешнего API

## ✨ Функционал

### Реализовано
- ✅ Каталог товаров с группировкой по категориям
- ✅ Фильтры по категориям и размерам
- ✅ Сортировка: от дорогих к дешевым / от дешевых к дорогим
- ✅ Страница детального просмотра товара
- ✅ Корзина покупок (localStorage)
- ✅ Модальное окно подтверждения при добавлении в корзину
- ✅ Смена фото при наведении на карточку товара
- ✅ Форма оформления заказа с выбором способа связи (Email/Telegram)
- ✅ Поддержка доставки в Россию и СНГ
- ✅ Интеграция с СДЭК (заглушка для виджета карты)
- ✅ **Система бронирования товаров на 20 минут**
- ✅ **Модальное окно оплаты с QR-кодом и таймером**
- ✅ **Автоматическая очистка истекших бронирований**
- ✅ **Визуальные индикаторы забронированных товаров**
- ✅ **Админ-панель с авторизацией**
- ✅ **Управление заказами (просмотр, изменение статуса)**
- ✅ **Управление товарами (просмотр, удаление)**
- ✅ **Загрузка изображений с ПК (обложка, спина, галерея)**
- ✅ **Статическая раздача файлов из backend**
- ✅ Адаптивный дизайн
- ✅ Анимации и hover-эффекты
- ✅ Импорт товаров из внешнего API
- ✅ Счетчик товаров в корзине с live-обновлением

### Дизайн
- Минималистичный стиль в духе vsrap.shop
- Шрифт Inter от Google Fonts
- Плавные переходы и анимации
- Современная цветовая палитра
- Качественные тени и градиенты

## 📱 Страницы

### Клиентская часть
- `/` - Главная (каталог с фильтрами)
- `/product/:id` - Страница товара
- `/cart` - Корзина
- `/checkout` - Оформление заказа

### Админ-панель
- `/admin/login` - Авторизация администратора
- `/admin/dashboard` - Панель управления (заказы и товары)
- `/admin/products` - Управление товарами (добавление, редактирование, удаление)
- `/admin/settings` - Настройки категорий и размеров

## 🎨 Компоненты

- `Header` - Шапка с логотипом, навигацией и корзиной
- `Footer` - Футер с контактами и ссылками
- `ProductCard` - Карточка товара
- `Filters` - Горизонтальная панель фильтров
- `ConfirmModal` - Модальное окно подтверждения
- `Home` - Главная страница с каталогом
- `ProductDetail` - Детальная страница товара
- `Cart` - Страница корзины
- `Checkout` - Страница оформления заказа

## 🔧 Разработка

### Backend
```bash
npm run start:dev  # Dev режим с hot reload
npm run build      # Сборка для продакшена
```

### Frontend
```bash
npm run dev        # Dev сервер
npm run build      # Сборка для продакшена
npm run preview    # Просмотр production сборки
```

## 📝 Структура проекта

```
silasvitera/
├── backend/
│   ├── src/
│   │   ├── products/          # Модуль товаров
│   │   ├── getsviter.controller.ts  # Импорт товаров
│   │   ├── app.module.ts
│   │   └── main.ts
│   ├── package.json
│   └── tsconfig.json
├── frontend/
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   ├── pages/             # Страницы
│   │   ├── App.tsx
│   │   └── index.tsx
│   ├── public/
│   ├── package.json
│   ├── webpack.config.js
│   └── tailwind.config.js
└── README.md
```

## 🎯 TODO

- [ ] Оформление заказа
- [ ] Админ-панель
- [ ] Авторизация пользователей
- [ ] Поиск по товарам
- [ ] Избранное
- [ ] Отзывы на товары
- [ ] Интеграция с платежными системами
- [ ] Email уведомления

## 📄 Лицензия

MIT

## 👤 Автор

Сила свитера - Магазин свитеров ручной работы
