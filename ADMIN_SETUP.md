# Настройка админ-панели

## 🚀 Быстрый старт

### 1. Установите зависимости backend
```powershell
cd backend
npm install
```

### 2. Запустите backend
```powershell
npm run start:dev
```

### 3. Запустите frontend (в другом терминале)
```powershell
cd frontend
npm run dev
```

### 4. Импортируйте товары
Откройте в браузере: `http://localhost:4000/getsviter`

### 5. Войдите в админ-панель
1. Откройте: `http://localhost:5173/admin/login`
2. Логин: `admin`
3. Пароль: `admin123`

## 📋 Функции админ-панели

### Дашборд
- Статистика: всего заказов, товаров, новых заказов
- Переключение между вкладками "Заказы" и "Товары"

### Управление заказами
- Просмотр всех заказов с деталями
- Изменение статуса заказа:
  - Новый (pending)
  - В обработке (processing)
  - Отправлен (shipped)
  - Доставлен (delivered)
  - Отменен (cancelled)
- Информация о клиенте (контакт, телефон)
- Сумма заказа

### Управление товарами
- Просмотр всех товаров
- Миниатюры изображений
- Цена и остаток на складе
- Удаление товаров

## 🔐 Безопасность

### Изменение пароля администратора

1. Сгенерируйте новый хеш:
```powershell
cd backend
node scripts/hash-password.js your_new_password
```

2. Скопируйте полученный хеш

3. Замените хеш в файле `backend/src/auth/auth.service.ts`:
```typescript
private readonly users = [
  {
    id: 1,
    username: 'admin',
    password: '$2b$10$YOUR_NEW_HASH_HERE',
  },
];
```

4. Перезапустите backend

## 🛠️ Разработка

### Структура файлов админ-панели

**Backend:**
- `src/auth/` - Модуль авторизации
  - `auth.controller.ts` - Контроллер авторизации
  - `auth.service.ts` - Сервис с логикой аутентификации
  - `dto/login.dto.ts` - DTO для логина
- `src/orders/` - Модуль заказов
  - `orders.controller.ts` - CRUD операции
  - `orders.service.ts` - Бизнес-логика
  - `schemas/order.schema.ts` - Mongoose схема
  - `dto/` - Data Transfer Objects

**Frontend:**
- `src/pages/admin/Login.tsx` - Страница авторизации
- `src/pages/admin/Dashboard.tsx` - Дашборд с управлением

### API Endpoints

**Авторизация:**
```
POST /api/auth/login
Body: { username: string, password: string }
Response: { access_token: string, user: { id, username } }
```

**Заказы:**
```
GET    /api/orders           - Все заказы
GET    /api/orders/:id       - Один заказ
POST   /api/orders           - Создать заказ
PATCH  /api/orders/:id       - Обновить заказ
DELETE /api/orders/:id       - Удалить заказ
```

## 💡 Советы

1. **Токен хранится в localStorage** - для production используйте JWT с refresh tokens
2. **Пароли хешируются с bcrypt** - соль 10 раундов
3. **Для production** добавьте:
   - JWT токены с истечением
   - Refresh tokens
   - Rate limiting
   - HTTPS
   - Роли и права доступа

## 🐛 Troubleshooting

### Ошибка "Неверный логин или пароль"
- Проверьте, что пароль `admin123` соответствует хешу в `auth.service.ts`
- Пересоздайте хеш через `hash-password.js`

### Не загружаются заказы/товары
- Проверьте, что MongoDB запущена
- Проверьте подключение в `.env`: `MONGODB_URI=mongodb://localhost:27017/silasvitera`
- Импортируйте товары через `/getsviter`

### Не работает авторизация
- Проверьте, что backend запущен на порту 4000
- Проверьте консоль браузера на ошибки
- Убедитесь, что bcrypt установлен: `npm install bcrypt`
